<br/>
<div class="col-sm-5" style="margin:0 auto;">
  <div class="card text-center"><br/>
    <h2 class="card-header text-center">ログイン</h2>
    <div class="card-body">
      <br/>

      <%= form_with model: @user, url: user_session_path, method: :post, remote: true do |f| %>

      <%= render "shared/error", obj:@user %>

        <div class="field">
          <%= f.label :メールアドレス, class: "label" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", size: 23 %>
        </div>
        <br/>

        <div class="field">
          <%= f.label :パスワード, class: "label" %><br />
          <%= f.password_field :password, autocomplete: "current-password", size: 23 %>
        </div>
        <br/>

        <% if devise_mapping.rememberable? %>
          <div class="field">
              <%= f.check_box :remember_me %>
              <%= f.label :ログイン状態を保持する %>
            </div>
            <br/>
        <% end %>

        <div class="actions">
        <%= f.submit "ログインする", class: "btn btn-success btn-block" %>
        </div>
        <br/>

      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>

<style>
  footer {
  width: 100%;
  height: 250px;
  background-color: 	#0dcaf0;
  display:none;
  }
</style>