<br />
<div class="col-sm-5" style="margin: 0 auto">
  <div class="card text-center">
    <br />
    <h2 class="card-header text-center">新規登録</h2>
    <div class="card-body">
      <br />

      <%= form_with model: @user, url: user_registration_path, local: true do |f| %> <% if @user.errors.any? %>
      <div id="error_explanation">
        <div class="alert alert-danger"><% @user.errors.full_messages.each do |message| %> <%= message %> <% end %></div>
      </div>
      <% end %>

      <div class="form-group">
        <%= f.label :ユーザー名, class: "label" %><br />
        <%= f.text_field :full_name, autofocus: true, size: 23 %>
      </div>
      <br />

      <div class="form-group">
        <%= f.label :メールアドレス, class: "label" %><br />
        <%= f.email_field :email, autofocus: true, size: 23 %>
      </div>
      <br />

      <div class="form-group">
        <%= f.label :パスワード, class: "label" %> <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> 文字以上)</em>
        <% end %><br />
        <%= f.password_field :password, size: 23%>
      </div>
      <br />

      <div class="form-group">
        <%= f.label :パスワード（確認用）, class: "label" %><br />
        <%= f.password_field :password_confirmation, size: 23 %>
      </div>
      <br />

      <div class="actions"><%= f.submit "登録する", class: "btn btn-success btn-block" %></div>
      <br />
      <% end %>
    </div>
  </div>
</div>

<style>
  footer {
    width: 100%;
    height: 250px;
    background-color: #0dcaf0;
    display: none;
  }
</style>
